# MIT License

target_sources(${FROG_TARGET}
    PRIVATE
        button.cpp
        camera.cpp
        node.cpp
        rect.cpp
        sdl_renderer.cpp
        texture_id.cpp
) # target_sources

# add_subdirectory(program)

# EOF
